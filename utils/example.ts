// utils/example.ts
// The "utils" folder is meant to store utility functions and helper modules.

export const formatGreeting = (name: string): string => {
  return `Hello, ${name}! Welcome to the application.`;
};
